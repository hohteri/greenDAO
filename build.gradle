subprojects {
    repositories {
        mavenCentral()
        jcenter()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}

ext {
    buildToolsVersion = '19.1.0'
    compileSdkVersion = 19 

    minSdkVersion = 4
    targetSdkVersion = 19
}
